<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#0b39ac" /> <!-- Fondo transparente -->
    <corners android:radius="5dp" /> <!-- Esquinas redondeadas -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Borde blanco -->
</shape>
